<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Paystack hosts the checkout, so the "form" is a bare GET to the
         authorization_url the transaction initialise call handed back. -->
    <template id="redirect_form">
        <form t-att-action="api_url" method="get"/>
    </template>

</odoo>
