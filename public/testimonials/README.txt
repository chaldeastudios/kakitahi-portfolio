Drop client headshots here, matching the filenames referenced in
src/lib/odoo/content.ts's ks-avatar values (e.g. saddiq.png, sonia.jpeg,
john.jpg). Git doesn't track empty directories, hence this placeholder —
delete it once real images are added, or just leave it.
